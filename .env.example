# Copy this file to `.env.local` and fill in values. Never commit `.env.local`.
# Required by `/api/translate`, `/api/tts` (MiniMax), and `/api/tts-poll`:
REPLICATE_API_TOKEN=
# Legacy Google and Mistral routes remain in the codebase for development only.
# The production interface uses Replicate for translation and voice.
